{
  "name": "root-delegator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "ng build",
    "dev": "cross-env ng serve --port=3000 --host=0.0.0.0 --allowed-hosts=all ${DISABLE_HMR:+--live-reload=false}"
  }
}
